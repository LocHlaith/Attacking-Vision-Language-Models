# 5 让模型将任何东西识别为厕纸

前文研究的攻击只要求模型不再将扰动后图像识别为原类别，并未规定模型最终应当输出哪一类别。本章进一步研究定向攻击：对于任意给定的原始图像，在其上增加尽可能小的扰动，使 MobileNetV2 最终将其识别为“厕纸”。

实验程序中“厕纸”类别的索引为 \(t=999\)。下文以程序中的索引为准，用 \(z_t(x)\) 表示模型对厕纸类别输出的 logit。与非定向攻击相比，本章不仅要降低原类别的竞争力，还必须使厕纸类别的得分超过其余所有类别。因此，直接以厕纸类别与最强竞争类别之间的得分差作为决策函数。

## 5.1 建立模型

设原始图像为 \(x\in\mathbb{R}^n\)，扰动为 \(\delta\in\mathbb{R}^n\)，扰动后图像为

$$
x'=x+\delta.
$$

定义厕纸类别的定向分类间隔函数

$$
M_t(\delta)
=z_t(x+\delta)-\max_{j\ne t}z_j(x+\delta),
\qquad t=999.
$$

当 \(M_t(\delta)>0\) 时，厕纸类别得分严格大于其他所有类别得分，模型将扰动后图像识别为厕纸。为避免不同类别得分相等时分类规则不确定，并使攻击结果保留一定余量，给定正的攻击阈值 \(\eta>0\)，要求

$$
M_t(\delta)\ge \eta.
$$

阈值 \(\eta\) 越大，要求厕纸类别领先其他类别的程度越高，通常也需要更大的扰动。以二范数平方衡量扰动规模，可以建立如下阈值约束非线性规划模型：

$$
\begin{aligned}
\min_{\delta\in\mathbb{R}^n}\quad & \|\delta\|_2^2\\
\text{s.t.}\quad & M_t(\delta)-\eta\ge 0.
\end{aligned}
$$

该模型限制模型对厕纸类别的最低决策间隔，并追求满足该要求的最小扰动。记约束函数为

$$
c_t(\delta)=M_t(\delta)-\eta,
$$

则模型可简写为

$$
\begin{aligned}
\min_{\delta\in\mathbb{R}^n}\quad & \|\delta\|_2^2\\
\text{s.t.}\quad & c_t(\delta)\ge 0.
\end{aligned}
$$

若原始图像已经满足 \(M_t(0)\ge\eta\)，则 \(\delta=0\) 是全局最优解。若阈值超过模型在合法输入范围内能够达到的最大定向分类间隔，则问题无可行解。因此，攻击阈值应兼顾攻击要求与问题的可行性。

由于各个 logit 是输入图像的非线性函数，且间隔函数中还含有最大值运算，该模型一般是非凸非线性规划问题。即使求得满足一阶必要条件的点，也通常只能将其视为局部最优解或满意解。

为与第4章的主要模型保持一致，上式首先只保留攻击阈值约束。数值实现时还应保证扰动后像素位于合法范围内，即

$$
0\le x_i+\delta_i\le 1,\qquad i=1,2,\ldots,n.
$$

该条件可通过增加约束处理，也可在每次迭代后将扰动后图像投影回合法像素范围。

## 5.2 数值求解

本章模型与第4章的攻击阈值约束模型结构相同，区别仅在于：第4章使用非定向攻击损失 \(J(x+\delta,y)\) 衡量攻击效果，本章使用厕纸类别的定向分类间隔 \(M_t(\delta)\) 衡量攻击效果。因此，可以直接沿用第4章的 Lagrange 松弛与双层数值迭代方法。

对约束 \(c_t(\delta)\ge 0\) 引入非负 Lagrange 乘子 \(\lambda\ge0\)，构造 Lagrange 函数

$$
L_t(\delta,\lambda)
=\|\delta\|_2^2-\lambda\bigl[M_t(\delta)-\eta\bigr].
$$

对于固定的乘子 \(\lambda\)，需要求解内层无约束问题

$$
\min_{\delta\in\mathbb{R}^n}
Q_\lambda(\delta)
=\|\delta\|_2^2-\lambda M_t(\delta).
$$

内层问题可采用第4章中的最速下降法求取局部极小点。记当前扰动下得分最高的非厕纸类别为

$$
r(\delta)=\arg\max_{j\ne t}z_j(x+\delta).
$$

当该类别唯一且各 logit 在当前点可微时，有

$$
\nabla M_t(\delta)
=\nabla z_t(x+\delta)-\nabla z_{r(\delta)}(x+\delta),
$$

从而

$$
\nabla Q_\lambda(\delta)
=2\delta-\lambda\nabla M_t(\delta).
$$

最速下降迭代为

$$
\delta^{(k+1)}
=\delta^{(k)}-\alpha_k
\left[2\delta^{(k)}-\lambda\nabla M_t\!\left(\delta^{(k)}\right)\right],
$$

其中 \(\alpha_k>0\) 为迭代步长，可通过一维搜索或其他合理的下降规则确定。当多个非厕纸类别并列取得最大得分时，间隔函数在该点可能不可微；实际计算中可从并列类别中选取一个类别计算方向，并继续进行数值迭代。

外层按照第4章的乘子调整方法更新 \(\lambda\)：

$$
\lambda^{(s+1)}
=\max\left\{
0,\ 
\lambda^{(s)}
+\rho_s\left[\eta-M_t\!\left(\delta(\lambda^{(s)})\right)\right]
\right\},
$$

其中 \(\rho_s>0\) 为外层步长，\(\delta(\lambda^{(s)})\) 表示第 \(s\) 次外层迭代对应的内层近似解。若当前定向分类间隔低于攻击阈值，则提高乘子，使下一次内层求解更加重视提高厕纸类别的得分；若当前间隔已经明显超过阈值，则乘子可能降低，使求解过程更加重视减小扰动。

由于内层目标函数一般非凸，最速下降法通常只能求得局部极小点。为减小初始点对结果的影响，可以采用多个初始扰动分别进行内层求解，并持续保留满足

$$
M_t(\delta)\ge\eta
$$

的可行候选。在全部可行候选中，选择二范数最小者作为最终满意解。

需要强调的是，与第4章相同，Lagrange 松弛中的内层运算在理论上要求求得全局最优解。若实际计算中以局部极小点代替全局最优解，则外层乘子更新只能视为近似求解过程，不能保证得到原问题的全局最优解。

## 5.3 算法流程与终止准则

综上，让模型将任意图像识别为厕纸的求解流程如下。

第一，读取原始图像 \(x\)，指定目标类别索引 \(t=999\)，并设置正的攻击阈值 \(\eta\)。

第二，初始化扰动、Lagrange 乘子、内外层步长与最大迭代次数。

第三，在固定乘子下，反复确定当前最强竞争类别，并采用最速下降法求解内层无约束问题；必要时将扰动后图像投影回合法像素范围。

第四，根据当前定向分类间隔与攻击阈值之差调整乘子。当间隔不足时提高乘子，当间隔超过阈值时允许降低乘子。

第五，在迭代过程中持续记录满足攻击阈值的可行扰动，并保留其中二范数最小者。

第六，当约束违反量

$$
\max\{\eta-M_t(\delta),0\}
$$

足够小，并且相邻两次迭代的扰动变化量或目标函数变化量足够小时停止；同时设置最大迭代次数，避免在非光滑点或局部极小点附近长期停滞。

本章算法在形式上与第4章相同，只将非定向攻击效果替换为面向厕纸类别的定向分类间隔。由于原问题非凸，算法不能保证得到全局最小扰动，但能够按照“先达到给定攻击阈值，再在可行候选中寻找最小扰动”的思想求得局部最优解或满意解。
